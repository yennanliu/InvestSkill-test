[pytest]
# Put the analysis package on the path so `import analysis` works from tests.
pythonpath = scripts
testpaths = scripts/tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: end-to-end tests that exercise the full CLI → pipeline → publish flow
